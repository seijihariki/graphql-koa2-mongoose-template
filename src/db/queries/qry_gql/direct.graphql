## All direct ID object queries

# Get company from ID
company(id: ID!) : Company!

# Get document from ID
document(id: ID!) : Document!

# Get person from ID
person(id: ID!) : Person!

# Get shift from ID
shift(id: ID!) : Shift!

# Get trip from ID
trip(id: ID!) : Trip!

# Get user from ID
user(id: ID!) : User!

# Get vehicle from ID
vehicle(id: ID!) : Vehicle!